// replace old word with new word && ensure that the english word is at first the visible one.
$('#english').html("<%= @word.english %>").css("display", "block");
$('#german').html("<%= @word.german %>").css("display", "none");
