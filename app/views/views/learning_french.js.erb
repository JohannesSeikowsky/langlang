// replace old word with new word && ensure that the english word is at first the visible one.
$('#english').html("<%= @word.french %>").css("display", "block");
$('#german').html("<%= @word.english %>").css("display", "none");

// tiny gamification feature
<%= session[:count] ||= 0 %>
<%= session[:count] += 1 %>
$('#gamification').html("<%= session[:count] %>");
